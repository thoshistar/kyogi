services:
  - type: web
    name: my-flask-app
    env: python
    plan: standard
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --workers=1 --threads=2 --timeout=300 app:app
